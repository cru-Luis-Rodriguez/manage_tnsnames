#file deliver via chef server, source at https://github.com/cru-Luis-Rodriguez/manage_tnsnames
#luis rodriguez

<%= @db_name %> =
  (DESCRIPTION =
    (ADDRESS = (PROTOCOL = TCP)(HOST = <%= @db_server %>)(PORT = 1521))
    (LOAD_BALANCE = yes)
    (CONNECT_DATA =
      (SERVER = DEDICATED)
      (SERVICE_NAME = <%= @service %>)
    )
  )
